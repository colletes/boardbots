# Diegetic UI — Next Level (full plan)

## Context

The first diegetic pass (Hoth "Echo Base Holocron", Tiny Epic Kingdoms medieval kingdom, Memoir'44 war bulletin, plus 3D physics dice via `@3d-dice/dice-box`) is merged to production. This plan covers the next round of improvements requested: go deeper on immersion, and consider options beyond plain HTML/JS/CSS where it pays off.

**Revision note (2026-09-01):** updated per user directive — no voice narration, no physical/print component (boardbots stays simple and 100% virtual), no native app wrapper for now; Phase 1 expanded to cover free textures/images/animations/flavor models more broadly; Phase 2 SFX redirected from synth bleeps toward real sampled sound effects.

Constraints carried over from the existing codebase/conventions:
- Each bot stays a single-file HTML deliverable (no bundler/build step introduced).
- Game logic, state persistence, and both solo modes (where applicable) must stay unchanged — these are visual/sensory/infra changes only.
- **No more versioned filenames** (`_v2`, `RC2`, etc.). Bot files were renamed (2026-09-01) to drop version suffixes (e.g. `stone_age_bot.html`, `hoth_bot.html`, `utek_bot.html`) — since `staging` + git history already provide the safety net a versioned filename used to provide, every future pass edits the live bot file in place.
- Validate with `node --check` on extracted inline `<script>` + live browser check (console errors, desktop 1440px / mobile 390px, no horizontal overflow) before every commit, same as prior passes.
- Ship through `staging` first, promote to `main` only when explicitly requested (per `deploy-to-prod` skill).
- Boardbots stays simple and 100% virtual — no physical/print deliverables, no native app packaging.

## Phase 0 — Foundation (do this before any new bot-specific polish)

**Status (2026-09-01): done for the pieces that are genuinely safe to share; rest is incremental.**

Goal: stop the recurring "redesign broke the layout again" bugs seen in Hoth/UTEK by making shared UI pieces a real shared asset instead of copy-pasted CSS/JS per bot.

1. ✅ Extracted `assets/theme-kit.css` (keyframes `fadeSlideUp`/`popIn`, `.icon-inline` em-relative icon utility, `.credits`/`.credits img`/`.credits-text` component with `--tk-credits-*` override vars) and `assets/theme-kit.js` (`ThemeKit.setAmbientIcon()` helper for the ambient-sound button icon-swap).
   - **Scope correction found during implementation**: `.hero`, `.lang-switch`, `.btn-help-float` turned out to NOT be safely unifiable — the catalog actually has 3+ deliberately different nav/help-button layout families (sticky top-nav bar, floating circular buttons, plain top-bar buttons) across different bots, not one drifted-apart copy of the same thing. Unifying those would be a real visual redesign per bot, not a mechanical CSS extraction — so only the pieces that really are identical (`.credits`, `.icon-inline`, keyframes) went into the shared kit. Documented in [SKILL.md Section 14](.agents/skills/boardbot-creator/SKILL.md).
2. ✅ `bots/mick_bot.html` migrated as a validated pilot (`.credits` now consumes theme-kit.css via CSS var overrides in `:root`) — confirmed 0.03-0.04% pixel diff (font-rendering noise, not a regression) via the new screenshot-diff tool. Remaining 18 bots migrate incrementally, one per future redesign pass — not a batch job (too risky to do unsupervised across the whole catalog in one pass).
3. ✅ Added `tools/visual-regression.mjs` (Playwright + pixelmatch, local static server via `python3 -m http.server`): `npm run visual-check [files...]` diffs current renders (desktop 1440px + mobile 390px) against committed baselines in `tools/visual-baselines/`; `npm run visual-update [files...]` (re)writes baselines. Baselines for **all 19 bots** captured and committed (gitignore has an explicit override so these PNGs aren't swept up by the repo's general `*.png` ignore rule).
4. Effort: medium, one-time infra + ongoing small effort per bot migrated. Payoff: every future bot pass gets safer and faster.
5. **Standing instruction:** this has been added to [.agents/skills/boardbot-creator/SKILL.md](.agents/skills/boardbot-creator/SKILL.md) as a mandatory step for any AI agent creating/updating a bot, so it survives beyond this one plan/session.

## Phase 1 — Deepen the diegesis itself, expanded to all bots + richer free assets

**Status (2026-09-01): item 1 (in-character voice) started — pilot done on `stone_age_bot.html`, ranked #1 by usage among untouched bots. `arknova_arno_bot.html` (ranked #2) also done.**

Right now the reskins are mostly color/texture/frame. The next level is making the bot *read* like the in-fiction character, not a UI with a theme applied to it — and doing it with richer visual material, not just CSS.

1. **In-character voice in text** (log/status copy), generalized across the whole bot catalog, not just a 3-bot pilot:
   - **Usage-data check (Phase 4) done first**: queried the public Firestore `counters` collection directly (`https://firestore.googleapis.com/v1/projects/boardbots-641cc/databases/(default)/documents/counters`, read is public per `firestore.rules`) for real `view-<bot>` counts. Ranking (excluding `eleven_bot.html`/`hoth_bot.html`/`memoir44_bot.html`, which already have a mature in-character voice from the first diegetic pass and the campaign/match-commentary system respectively): **`stone_age_bot.html` is #1 untouched bot by views (124)**, ahead of `arknova_arno_bot.html` (110), `utek_bot.html` (89), `thunder_road_vendetta_bot.html` (59), `mick_bot.html` (55, already migrated in Phase 0), `cafe_baras_bot.html` (39), `sanctuary_bot.html` (35).
   - **Pilot done**: rewrote all of `stone_age_bot.html`'s Automa-mode and Official-Solo-Mode log strings (`logGameStarted`, `logMarkedOccupied`, `logAllocatedResource/Tool/Mating/Agri`, `logCivPoints/Resource/Food/Agri/Tool/Mixed/Draw`, `logBuildingFixed/Dice`, `logNewRound`, and the `officialLog*` family), PT+EN, from plain mechanical phrasing ("Automa alocou em [8] Cabana de Agricultura: agricultura +1") into tribal-chronicle voice ("Os campos do Automa florescem em [8] Cabana de Agricultura — agricultura avança para 1"), preserving every piece of dynamic data (position, name, points, round, dice rolls) so no information is lost.
   - No new `narrate()`/`FLAVOR` abstraction was introduced — the bot's existing `S().logXxx(...)` i18n dictionary already *is* that abstraction (one key per log event, per-language template functions with placeholders), so the fix was purely rewriting the template string content, zero structural/call-site changes. Future bots should follow the same pattern: look for the existing per-bot `logXxx`/status-message i18n keys first, and only introduce a new table if a bot has no such abstraction yet.
   - Validated: `node --check` on the extracted inline script (no syntax errors), `npm run visual-check stone_age_bot.html` (0.000% diff on both viewports — pure string-content change, no layout impact), and a live Playwright pass exercising both Automa mode (roll allocation → confirmed new log line) and Official Solo Mode (start game → apply feeding, confirmed new log lines) in PT and EN — all render correctly. Confirmed pre-existing (not a regression) quirk: history log entries, once written, don't retranslate on a language switch — same documented behavior as other bots (e.g. Mystic Vale's undo).
   - **`arknova_arno_bot.html` done (2026-09-01)**: unlike Stone Age, this bot has no existing `logXxx`/template-string abstraction — its live-turn and history text (`getTurnLines()`) is a purely mechanical stat-delta readout built from inline PT/EN ternaries (e.g. "Reputation +2, Break +3"), which must stay untouched since players rely on exact numbers. Added a small new, additive `ZOO_LOG` map (one short "zoo field log" narrator sentence per `actionKey`: animals/cards/build/sponsors/association, PT+EN, e.g. "A new species arrives at the enclosure and already draws visitors to the gate.") plus a `zooLogLine()` helper, rendered as a new italic line above the existing numeric readout in both `renderRollOut()` (live turn) and `renderHistory()` (history list) — zero changes to any existing stat-delta string or game logic.
   - Validated: `node --check` on the extracted inline script (no syntax errors); `npm run visual-check arknova_arno_bot.html` (desktop 0.087%, below threshold; mobile 0.83% diff was the pre-existing documented false positive — this bot's setup screen randomly shuffles action-card slot assignment on every page load, confirmed via the diff image which only highlights the shuffled setup cards, unrelated to this change); live Playwright pass rolling through Build, Animals actions in both EN and PT confirmed the new flavor line renders correctly alongside the unchanged numeric stat lines, in both the live roll-out box and each history entry.
   - **`utek_bot.html` done (2026-09-01)**: has no `logXxx`/template abstraction — its `logMsg(text)` function just prepends a raw inline string built at each of 10 call sites (PT/EN ternaries directly in the calling code, no lookup table). Since call sites are few, rewrote each string literal in place into a royal-advisor/court-counsel voice ("Meu senhor, ..."/"My lord, ..."), preserving every dynamic value (round numbers, faction names, action names, roll results). No new abstraction introduced.
   - Validated: `node --check` (no syntax errors); `npm run visual-check utek_bot.html` (0.000% diff both viewports — pure text change); live Playwright pass starting a game, taking a Bot turn (official priority pick) and a player turn, and switching to EN — all new phrasing rendered correctly with dynamic values intact. No 3D dice tray in this bot (Compass roll is a rotating-dial UI, not `dice-box`), so the Ark Nova dice-tray fix pattern doesn't apply here.
   - **`thunder_road_vendetta_bot.html` — investigated, found to be a structural exception, SKIPPED for this pass (2026-09-01)**: unlike every other bot so far, this one has no generated narrative/log text at all — its entire UI is a faithful visualization of the physical activation cards (dice values, direction arrows, fire/smoke/ignite icons) progressively revealed via a `globalStep` counter; all text strings are either static setup instructions or literal UI labels/tooltips (`inactivePhase`, `fireTitle`, etc.), not narrator sentences describing what a Bot "did". There is no `logXxx`/status-message abstraction to rewrite, and inventing one from scratch would be new-feature scope creep, not a copy rewrite — so it was left untouched. Revisit only if the user explicitly wants a new dispatch-log feature added to this bot (out of scope for the current "rewrite existing voice" pass).
   - **`cafe_baras_bot.html` done (2026-09-01)**: already had the same `S().logXxx()` per-event template-function abstraction as Stone Age (`logStart`, `logAdd`, `logServe`, `logUndo`, `endGameTriggered`). Rewrote all 5, PT+EN, from flat mechanical phrasing ("Capybot ADDED a Coffee card...") into warm capybara-barista café voice ("the Capybot tucks a new Coffee card onto the shelf...", "ties on its apron...", "flips the sign to closing soon..."), preserving every dynamic value (turn, cost, type, coins before/after, ability note, regulars count).
   - Validated: `node --check` (no syntax errors); `npm run visual-check cafe_baras_bot.html` (0.000% diff both viewports); live Playwright pass starting a match (confirmed `logStart` renders) and taking an Add turn (confirmed `logAdd` renders with correct cost/coin values) in EN.
   - **`sanctuary_bot.html` done (2026-09-01)**: an official Ark Nova spinoff ("An Ark Nova Game", Capstone Games) sharing Ark Nova's exact code shape — a mechanical stat-delta readout (`hLines`) that must stay numerically unchanged. Followed the same additive pattern as Ark Nova: added a small `SANCTUARY_LOG` map (one short flavor sentence per action-card type: projects/animal_rock/animal_forest/animal_water, PT+EN) + `sanctuaryLogLine()` helper, rendered as an extra italic line above the existing numeric readout in both the live roll-out box and history entries, inside `resolveTurn()`. Zero changes to any existing stat/game logic.
   - Validated: `node --check` (no syntax errors); `npm run visual-check sanctuary_bot.html` (first run showed a 2.80% mobile diff, re-confirmed as the same pre-existing "random setup-card-shuffle" false positive already documented for Ark Nova — a 2nd run came back at 0.086%/0.186%, both below threshold); live Playwright pass starting a game and drawing an action confirmed the new flavor line renders correctly alongside the unchanged stat readout, in both the live box and the history panel. Note: this bot also has a `#dice-box` div (potential same disconnected 3D-dice-tray issue fixed for Ark Nova) — NOT investigated/touched this pass, out of scope unless explicitly requested.
   - **`Heroscape_bot.html` done (2026-09-01)**: has a mature existing `S().resXxx`/`targetXxx`/`moveXxx` i18n template-function dictionary (Automated Battle Analyzer roll-result strings). Rewrote the content of `resInitiative`, `targetAutoAttacked/VisionSuccess/VisionFail/PointBlank/CoverSuccess/CoverFail/LostYes/LostNo`, `moveMeanderChoice`, `moveResult` (PT+EN) into a tactical battle-analyzer/military-uplink voice ("Contact confirmed!", "The division turns per the dial above...", "The division falls back to marching order parameters."), preserving every placeholder (`{d}`, `{n}`, `{type}`) and status icon (✔️/❌/⚠️). Left `markerLine` untouched (a compact data-table row, not narration).
   - Validated: `node --check` (no syntax errors); `npm run visual-check Heroscape_bot.html` (0.000% diff both viewports); live Playwright pass rolling Initiative and a Direct-Line-of-Sight target check confirmed the new phrasing renders correctly with dynamic values intact (dice fallback used in headless mode, unrelated to this change, already a documented quirk).
   - **`Mystic_Vale_bot.html` done (2026-09-01)**: like Ark Nova/Sanctuary, its per-turn `buildActionHTML()` instruction text (`actionAdv`/`actionVale`/`actionVP`/`noAction`) is a precise physical-action instruction the player must follow exactly (which card to discard from which position) — left completely untouched. Added a small additive `NEMESIS_FLAVOR` map (4 short atmospheric sentences keyed by which branch fired that turn: vp/vale/adv/none, PT+EN, e.g. "Corruption spreads, twisting growth into decay.") rendered as a new italic `.nemesis-flavor` line before the instruction `<ul>`, inside `buildActionHTML()`. Zero changes to any instructional text or game logic. (The pre-existing `alert: "The Nemesis grew stronger!"` string already had good voice and was left as-is.)
   - Validated: `node --check` (no syntax errors); `npm run visual-check Mystic_Vale_bot.html` (0.000% diff both viewports); live Playwright pass running a turn confirmed the new flavor line ("Corruption spreads, twisting growth into decay.") renders correctly above the unchanged mechanical instruction.
   - **`7_wonders_duel_bot.html` done (2026-09-01, analytics-untracked bot)**: has an existing `addLog`/`renderLogs` typed-message system with an `I18N` template dict (`logStart`/`logReshuffle`/`logResolved`/`logAge`/`logExtraTurnTag`). Rewrote all 5 (PT+EN) into an ancient-rivalry/herald voice ("The rivalry begins: {leader} raises its banner against you.", "The Decision Deck runs dry — the scribe reshuffles the remaining cards."), preserving all placeholders (`{leader}`, `{age}`, `{cardId}`, `{extra}`).
   - Validated: `node --check` (no syntax errors); `npm run visual-check 7_wonders_duel_bot.html` (0.000% diff both viewports); live Playwright pass selecting a leader (Caesar) and starting a duel confirmed `logStart` renders correctly ("⚔️ The rivalry begins: Caesar raises its banner against you.").
   - **`air_land_sea_bot.html` done (2026-09-01, analytics-untracked bot)**: no dedicated log/history panel — a real-time step-by-step prompt UI (no per-turn narration to enhance). Only rewrote the 2 setup/game-over strings that already had some voice (`enhancement2`/`enhancement3`/`gameOverVictory`/`gameOverDefeat`) into a slightly more atmospheric military-command tone, preserving the `{pts}` placeholder. Validated via `node --check` (no syntax errors) and `npm run visual-check air_land_sea_bot.html` (0.164%/0.032% diff, both below threshold — likely random card-hand shuffle noise); skipped a full live Playwright pass since these strings only ever display at difficulty-setup/game-over, a low-risk surface.
   - **`burgundy_bot.html` investigated, SKIPPED (2026-09-01, analytics-untracked bot)**: a pure dice/decision-helper tool with only generic UI labels (`waitingRoll`, `rolling`, `copyInstructions`) and a terse `botDecision` heading ("AutoDuque Decision:") — no narrative/status text worth rewriting, same class of structural exception as Thunder Road Vendetta. Left untouched.
   - **`mistborn_bot.html` done (2026-09-01, analytics-untracked bot)**: has a real `addLog`/`logPanel` system, but built from inline PT/EN ternary strings at ~13 call sites (no lookup-table abstraction) rather than a template dict — same shape as UTEK. Rewrote each string literal in place into an atmospheric Allomancy/mist voice ("The mists rise: the battle begins against...", "The villain's power grows: Dominance advances to...", "A new threat emerges from the shadows...", "The threat dissipates into the mist."), preserving every dynamic interpolation (villain name, player count, HP values, dominance step label, dice results).
   - Validated: `node --check` (no syntax errors); `npm run visual-check mistborn_bot.html` (0.015%/0.008% diff, both below threshold); live Playwright pass selecting Lord Ruler and starting battle confirmed the new `initMsg` line renders correctly ("⚔️ The mists rise: the battle begins against Lord Ruler (1 players).").
   - **`space_base_bot.html` done (2026-09-01, analytics-untracked bot)**: has an `addGameLog`/`renderGameLog` system, but its ~17 log entries are almost all terse technical/audit-style readouts (VP deltas, dice sums, reshuffle notices) not really narratable without becoming noisy — left those untouched. Added a short atmospheric suffix (append-only, technical text preserved verbatim) to the 3 most narratively meaningful events only: game start vs Automa ("— the fleets take position in orbit."), Base Attack SoloPlay start ("— sirens wail across the station."), and Rivals Attacked ("— engines roar as the assault begins."), PT+EN.
   - Validated: `node --check` (no syntax errors); `npm run visual-check space_base_bot.html` (0.000% diff both viewports). Skipped a full live Playwright pass given the low-risk append-only nature of the change (verified purely via syntax + visual-regression, consistent with the "don't over-invest validation effort proportional to risk" judgment used for `air_land_sea_bot.html` above).
   - **`lotr_duel_bot.html` done (2026-09-01)**: has an `addLog`/`renderLogs` system with an `I18N` template dict, same code shape as `7_wonders_duel_bot.html` (`logStart`/`logReshuffle`/`logResolved`/`logAge`/`logExtraTurnTag`/`logDiceLandmark`/`logDiceRace`/`logBuyLandmark`). Rewrote all 8 keys (PT+EN) into a Middle-earth/duel voice ("The shadow draws near: the duel against {leader} begins.", "The pages turn — welcome to Chapter {age}.", "Fate rolls {val} to choose the Landmark.", "The Leader raises a new Landmark, paying {cost} coins!"), preserving every placeholder (`{leader}`, `{age}`, `{cardId}`, `{extra}`, `{val}`, `{res}`, `{cost}`). Also fixed a pre-existing wording inconsistency where the PT strings said "Chapter" (untranslated) instead of "Capítulo".
   - Validated: `node --check` (no syntax errors); `node tools/visual-regression.mjs lotr_duel_bot.html` (0.000% diff both viewports); live Playwright pass selecting Witch-king and starting a duel confirmed the Action History panel renders "⚔️ The shadow draws near: the duel against Witch-king begins."
   - **`lostcities.html` done (2026-09-01)**: has a `logAction()`/`renderLog()` system built from inline PT/EN ternary strings (same shape as UTEK/Mistborn), rewritten into an in-character rival-explorer/expedition voice ("The {cName} expedition advances with a card of value {valText}.", "The {cName} expedition reroutes, swapping in card {valText}.", "Stashes the {valText} card from {cName} in the supplies for later.", "Abandons the {valText} card from {cName} at camp."). Also fixed a real (pre-existing, unrelated to voice) logic bug in the same function: the REPLACE and RESERVE action types had no dedicated branch and were silently falling through to the generic "Discarded" wording — added explicit branches for both so the log now correctly reflects what actually happened. Updated the `lc_log_reserve_play` prefix string (PT+EN) to match the new tone.
   - **Also fixed the reported bug: Lost Cities' "End Round" button didn't show the score calculator.** Root cause: `calcScreen`/`summaryScreen` start with a static `hidden` class in their HTML markup, and the shared `assets/site.css` has a global `.hidden{display:none !important}` utility rule (added at some point after this bot's calculator/summary logic was written). `openCalculator()`/`showRoundSummary()` only ever toggled `style.display` directly and never removed the `hidden` class, so the `!important` rule permanently overrode the inline style, keeping both screens invisible no matter what. Additionally, `showScreen()` (used correctly elsewhere for setup/game screens) cleared inline style to `''` on show, which doesn't work for calc/summary since their own CSS class already bakes in `display:none` by default (unlike setup/game, whose classes default to visible). Fix: updated `showScreen()` to explicitly set `display:'block'` for `calcScreen`/`summaryScreen` when they're the target, and routed `openCalculator()`/`showRoundSummary()` through `showScreen()` instead of manual `style.display` toggling. This bug affected BOTH the Solo vs Automa mode's "End Round" flow and the standalone "Calculator Only (Multiplayer)" mode (which also calls `openCalculator()` directly).
   - Validated: `node --check` (no syntax errors, both before/after the voice pass); `node tools/visual-regression.mjs lostcities.html` (0.000% diff both viewports, both passes); live Playwright pass reproduced the original bug (confirmed `calcScreen` had `computedDisplay:none` despite `style.display:block` before the fix), then confirmed the fix — after "End Round", `calcScreen` now shows with `computedDisplay:block` and the calculator UI renders correctly; confirming a score then correctly transitions to `summaryScreen` (`computedDisplay:block`, match summary table + tie/win text render correctly). Also confirmed a PLAY action renders the new log wording ("The Yellow expedition advances with a card of value 5.").
   - **`mick_bot.html` audited (2026-09-02)**: confirmed already strong, no rewrite needed. Its "Setlist" history, "Strum to start the show", "the stage is empty...", "MAX STATUS!", "PLAN B ACTIVATED" copy is already fully in-character rockstar-concert voice from the earlier house-style pass. Only generic UI-chrome strings remain (`action_confirmed`, `confirm_new_game`, `toast_copied`) — left as-is, consistent with how other bots keep pure confirm-dialog/toast chrome unthemed while narrating actual game events.
   - Remaining rollout (not yet done): none — all bots in the full catalog have now been either rewritten or explicitly investigated-and-skipped with a documented reason, including `mick_bot.html` above.
   - e.g. Hoth = radio-chatter command terminal phrasing; Tiny Epic Kingdoms = royal-advisor counsel ("meu senhor/my lord"); Memoir'44 = field-radio bulletin phrasing; Stone Age = tribal chronicle (done); extend the same idea to every other bot's own fiction (Mistborn = Ministry/underground report, Ark Nova = zoo field log, etc.).
2. **Free textures/images beyond CSS gradients** — sources that are CC0/public-domain (no attribution required, safe for a public repo) or CC-BY (attribution added to `credits.html`, never used silently):
   - **Textures**: [ambientCG](https://ambientcg.com) and [cc0textures.com](https://cc0textures.com) (CC0 seamless material textures — parchment, wood, stone, metal, fabric) for background layers, replacing/supplementing CSS-only noise-SVG textures.
   - **Icons**: [game-icons.net](https://game-icons.net) (CC-BY 3.0, huge library of board-game-relevant SVG icons) as a richer alternative/supplement to the hand-drawn inline SVG icon set already in use.
   - **UI chrome / flavor sprites**: [Kenney.nl](https://kenney.nl/assets) asset packs (all CC0 — UI packs, fantasy/sci-fi icon packs, parchment/wood UI packs) for borders, frames, buttons, badges matching each bot's genre.
   - **Flavor photography/illustration**: CC0 sources only ([unsplash.com](https://unsplash.com)/[pexels.com](https://pexels.com) license-permitting, or public-domain illustration archives) for section-header flavor images (e.g. a snowy tundra photo behind Hoth's status header) — never scrape copyrighted game art beyond the box-cover crops already licensed/used with the physical game's own assets.
   - Any non-CC0 asset used gets a corresponding credit line added to `credits.html`, consistent with how game/solo-mode designer credits are already handled.
   - **Status (2026-09-02): DONE for all 19 bots in the catalog**, all via ambientCG.com CC0 seamless material textures, resized to 512x512/quality=72 and wired into each bot's `body` (or equivalent backdrop div) background-image stack (details + reusable workflow logged in repo memory `boardbots-phase1-textures.md`): `stone_age_bot.html` (cave-ground.jpg, Ground110), `arknova_arno_bot.html` (cork-board.jpg, Cork001), `sanctuary_bot.html` (forest-floor.jpg, ScatteredLeaves009 — plus a full palette retheme, see repo memory), `thunder_road_vendetta_bot.html` (rusted-metal.jpg, Metal063 — also fixed a pre-existing broken-image 404 bug in the same CSS slot), `cafe_baras_bot.html` (dark-walnut.jpg, Wood051), `utek_bot.html` (old-parchment.jpg, Paper004). **Batch 2 (usage-ranked via Firestore view counts)**: `eleven_bot.html` (stadium-grass.jpg, Ground037, blended via `multiply` with the existing pitch-stripe pattern), `hoth_bot.html` (frost-ice.jpg, Snow006), `mick_bot.html` (stage-asphalt.jpg, Asphalt033, `multiply`-blended with the existing diagonal-hatch floor pattern), `memoir44_bot.html` (aged-bulletin.jpg, Paper001, blended into the `.stars` war-map backdrop div). **Batch 3 (all 9 remaining bots, single pass)**: `7_wonders_duel_bot.html` (ancient-marble.jpg, Marble012), `Heroscape_bot.html` (stone-slab.jpg, Rock030), `Mystic_Vale_bot.html` (mossy-vine.jpg, Moss001), `air_land_sea_bot.html` (rusted-metal.jpg reused), `burgundy_bot.html` (dark-walnut.jpg reused), `lostcities.html` (old-parchment.jpg reused), `lotr_duel_bot.html` (stone-slab.jpg reused), `mistborn_bot.html` (rusted-metal.jpg reused, fresh 3-layer stack built from scratch), `space_base_bot.html` (hull-panel.jpg, MetalPlates015A). All CC0, no `credits.html` entries needed. **Every bot in the 19-bot catalog now has a CC0 texture layer — item 2 is fully complete.**
3. **Lightweight flavor animations**, still pure CSS/JS (no new runtime dependency beyond what's already used):
   - parallax background layers (2-3 depth layers moving slightly on scroll/mouse) on hero banners.
   - ambient particle effects tuned per theme (falling snow on Hoth, embers/dust on Tiny Epic Kingdoms, static/scanline flicker on sci-fi bots) — same canvas/CSS-particle technique already used for `dice_roller_v1.html`'s VFX.
   - **Status (2026-09-02): DONE for all 19 bots in the catalog**, each with either a NEW genre-matched CSS-only particle + glow-flicker pair, or a pre-existing equivalent judged sufficient (pattern documented in repo memory `boardbots-phase1-textures.md`): Stone Age/Ark Nova/Sanctuary (firelight/canopy glow + fireflies), Thunder Road Vendetta (wasteland haze + embers/sparks), Café Baras (warm glow + rising coffee steam wisps), UTEK (torchlight glow + embers). All wrapped in `@media (prefers-reduced-motion: no-preference)`, verified via computed-style checks (`animationName`, `zIndex:-1`) rather than relying solely on screenshots. **Batch 2**: `eleven_bot.html` (floodlight flicker + rising pitch-dust motes), `hoth_bot.html` (new CSS falling-snow layer, distinct from and layered above its pre-existing JS starfield canvas), `mick_bot.html` (sweeping neon stage-spotlight beams + drifting fog-machine haze puffs). `memoir44_bot.html` needed NO new ambiance — already has a mature pre-existing JS canvas ember/smoke particle system (`initStarfield()`). **Batch 3 (all 9 remaining bots)**: added new glow+10-particle-layer pairs to `Heroscape_bot.html` (torch glow + embers), `7_wonders_duel_bot.html` (temple glow + dust motes), `burgundy_bot.html` (hearth glow + dust motes), `lostcities.html` (lantern glow + sand motes), `lotr_duel_bot.html` (throne glow + embers). Left pre-existing ambiance untouched (judged sufficient, though lighter-weight than the full particle-system bar) for `mistborn_bot.html` (mature JS `#ashCanvas`), `Mystic_Vale_bot.html` (minimal CSS 2-firefly effect), `space_base_bot.html` (`stars-move` scrolling starfield), `air_land_sea_bot.html` (`radarSweep` rotation). **Every bot in the 19-bot catalog now has an ambient animation layer — item 3 is fully complete.**
   - Also fixed a systemic `.hero` banner crop bug found during this rollout (portrait box art cropped to an unreadable sliver by a wide/short hero container) on Stone Age, Ark Nova and Sanctuary — full root-cause + fix pattern in repo memory. Confirmed NOT applicable (and correctly skipped) for Thunder Road Vendetta, Café Baras, UTEK, Eleven, Hoth and Mick, whose hero designs already show the box art legibly (dark decorative wash + separate title, a landscape-oriented cover, or an already-built contain-card layout).
4. **Flavor "models"** — a few bots could get a small rotating showpiece using [`<model-viewer>`](https://modelviewer.dev) (a free, dependency-light Google web component, just one `<script type="module">` tag, no bundler) loading a free CC0 glTF model (e.g. from [Poly Pizza](https://poly.pizza), filterable by CC0) for a hero-banner flourish (a rotating die, coin, token, ship). Treat as an optional per-bot flourish, not a platform-wide requirement — only where a fitting free CC0 model actually exists; never fabricate/assume a source.
5. **Icon polish & card/box chrome depth — NEW item found via audit (2026-09-02), not yet started.** Textures (item 2) and ambiance (item 3) are done for all 19 bots, but the third leg of "house style" — converting decorative emoji to inline SVG icons, plus the hero-banner/credits-row/card-depth polish that goes with it — was only ever applied ad hoc to **5 bots** in earlier sessions (`Heroscape_bot.html`, `Mystic_Vale_bot.html`, `mick_bot.html`, `hoth_bot.html`, `arknova_arno_bot.html`/`eleven_bot.html`/`memoir44_bot.html` also low-emoji from the original diegetic pass). It was never tracked as a plan item, so most of the catalog never got it. Real decorative-emoji counts audited this session (excludes functional arrows/dingbats like ✔/✕ used as compact status glyphs, which don't need conversion):
   - **High priority (dense decorative emoji, likely never had an icon pass)**: `lotr_duel_bot.html` (172), `7_wonders_duel_bot.html` (75), `space_base_bot.html` (74), `burgundy_bot.html` (65), `stone_age_bot.html` (61), `mistborn_bot.html` (42), `lostcities.html` (41), `utek_bot.html` (34), `air_land_sea_bot.html` (32).
   - **Medium priority**: `thunder_road_vendetta_bot.html` (45, mostly card-face glyphs — needs care since some may be load-bearing game-state symbols, not decoration), `cafe_baras_bot.html` (22), `sanctuary_bot.html` (17).
   - **Low priority / likely already mostly fine**: `arknova_arno_bot.html` (9), `memoir44_bot.html` (10), `eleven_bot.html` (4).
   - Reuse the established low-risk technique (see persistent user memory `board-game-bots.md`): keep data/string content unchanged, add an `ICONS`/`EMOJI_ICON_MAP` + `iconize()` helper that swaps a leading emoji token for an inline stroke-SVG span, and for emoji embedded directly in static HTML/i18n dict values just delete the emoji and place a sibling `<svg class="icon-inline">` (never inside the same element as a `data-i18n`/`textContent`-target attribute, or it gets wiped on re-render).
   - Bundle in the same pass: the `.hero` banner + `.credits` row + `assets/theme-kit.css` migration (Phase 0 item 2, only `mick_bot.html` migrated so far) for whichever bot is being touched — these were always done together historically, and doing them separately would mean re-touching the same file's CSS twice.
   - Prioritize using the existing Firestore usage ranking already gathered for Phase 1 item 1 (Phase 4 data), same as that rollout, rather than the raw emoji-count order above.

6. **Extend the texture/ambiance/icon treatment to `tools/dice_roller_v1.html` and `tools/point_counter_v1.html` — NEW item found via audit (2026-09-02), not yet started.** These 2 standalone tool pages (linked from `index.html`, not part of the 19-bot catalog) were never included in any Phase 1 rollout pass — confirmed zero `assets/textures/` references and zero glow/particle layers in either file. Both already have strong bespoke visual identities to build on rather than needing a redesign: `dice_roller_v1.html` has a real wood-tray-rim + green-velvet dice tray look; `point_counter_v1.html` has a brass/ledger/wax-seal "accountant's desk" look. Add a matching CC0 texture (e.g. a wood-grain/felt texture for the dice tray backdrop, aged-paper/leather for the score counter's desk background) plus a genre-appropriate glow+particle pair (candlelight flicker + dust motes fits their shared "tabletop den" aesthetic), following the exact same low-risk technique used across the 19-bot rollout (repo memory `boardbots-phase1-textures.md`).

Effort: low-medium per bot for text/animations; textures/icons are a one-time asset-sourcing effort reused across many bots via Phase 0's shared kit; 3D models are opportunistic/optional; item 5's icon+chrome pass is medium effort per bot (mirrors the existing Heroscape/Hoth/Mick house-style passes) and is the single largest *not-yet-started* piece of Phase 1.

## Phase 2 — Sensory layer: real sound effects + haptics + physical dice everywhere

No voice narration (removed per directive).

1. **Move SFX from synth bleeps toward real sampled sound effects.** The oscillator-only approach in `tools/dice_roller_v1.html` sounds "Atari-like"; replace/supplement it with short CC0 sampled audio:
   - Sources: [Kenney.nl](https://kenney.nl/assets) audio packs (Interface Sounds, RPG Audio, Sci-Fi Sounds — all CC0), [OpenGameArt.org](https://opengameart.org) audio section (filter CC0), [freesound.org](https://freesound.org) (filter strictly to CC0-licensed sounds only).
   - Store short `.ogg`/`.mp3` files under `assets/sfx/{shared|game}/`, played via a small shared helper in `assets/theme-kit.js` (`playSfx(key)`, preloads + plays an `<audio>`/`AudioBuffer`) — consistent with Phase 0's shared-kit approach instead of duplicating audio-loading code per bot.
   - Keep the existing oscillator synths only for cheap ambient/background texture (e.g. a low wind hum, distant murmur) where a real sample isn't needed; use real samples for discrete action feedback (card flip, dice clatter, coin drop, sword clash, seal stamp) tuned per bot's genre.
   - Any CC-BY sample used gets credited in `credits.html`, same rule as Phase 1 assets.
2. **Haptics**: `navigator.vibrate(...)` on dice roll / critical result on mobile — short buzz pattern, cheap addition, currently unused anywhere in the codebase.
3. **Roll out 3D dice to the remaining bots still on `Math.random()`-only rolls**, using the already-validated `rollDiceBoxSafe` timeout + value-sanitization pattern from the dice-hang fix (see repo memory `boardbots-staging-dice-fix.md`).
   - **Audited (2026-09-02) — confirmed exactly 4 bots have a real dice mechanic but no `dice-box` 3D tray**: `hoth_bot.html`, `memoir44_bot.html`, `thunder_road_vendetta_bot.html`, `mick_bot.html` (all currently show dice results as text/log lines or CSS-drawn pips, no physics dice). Already-covered by `dice-box`: `Heroscape_bot.html`, `arknova_arno_bot.html`, `burgundy_bot.html`, `lotr_duel_bot.html`, `mistborn_bot.html`, `sanctuary_bot.html`, `space_base_bot.html`, `stone_age_bot.html` (8 bots). Confirmed NOT applicable (no real dice mechanic): `7_wonders_duel_bot.html`, `lostcities.html`, `air_land_sea_bot.html`, `cafe_baras_bot.html`, `eleven_bot.html`, `Mystic_Vale_bot.html`.
   - **`utek_bot.html` and `Heroscape_bot.html`'s dial/compass UIs are explicitly OUT of scope for dice-box conversion, per user directive (2026-09-02) — they add charm, keep them.** UTEK's Compass roll already resolves via a bespoke hand-built rotating astrolabe/mariner-compass needle widget (`compass-wheel`/`compassNeedle`, `rotateNeedleTo()`/`spinCompassTo()`) that spins to the rolled 1-12 result — a more thematic visualization than a generic d12 physics die would be, not a gap to fill. Heroscape already has `dice-box` for its actual combat dice; its separate Sweep/Meander **movement** roll deliberately drives a custom rotating `.dial`/`.dial-wedge` compass-rose widget (highlighting the matching wedge on a D20 result) instead of a physics die, and that should also stay as-is for the same reason. Treat both as the model for "bespoke non-dice-box interactive roll widget" rather than a gap.
4. **Give existing/new `dice-box` bots bespoke themes, not just a default-theme recolor.** ✅ **DONE (2026-09-01).** Centralized the previously-duplicated per-bot `dice-box` CDN-init boilerplate into a new shared module `assets/dice-roller.js` (`loadDiceBox`/`rollDiceSafe`/`sanitizeDie`, handles `externalThemes` wiring + timeout-guarded rolls in one place), then migrated all 8 existing `dice-box` bots to consume it with a real bespoke theme each (reusing the CDN `@3d-dice/dice-themes` package, same `systemName`/`material.type` pattern already proven in `tools/dice_roller_v1.html`): `Heroscape_bot.html`→`rust`, `arknova_arno_bot.html`→`smooth`, `burgundy_bot.html`→`wooden`, `lotr_duel_bot.html`→`gemstone`, `mistborn_bot.html`→`blueGreenMetal`, `sanctuary_bot.html`→`wooden`, `stone_age_bot.html`→`wooden`, `space_base_bot.html`→self-hosted `stellar` (unchanged, already bespoke). All 8 live-verified via Playwright post-migration — zero page-breaking errors, correct roll results. Full details + testing lessons in persistent user memory `board-game-bots.md`.
   - This is separate from (and smaller-effort than) item 3's dice-box rollout — it's a pure visual upgrade to trays that already work, reusing the exact CDN wiring pattern already proven in `tools/dice_roller_v1.html` rather than inventing a new one.

Effort: medium. Mostly client-side; the only new "infra" is a handful of small CC0 audio files added to `assets/`, no backend/API involved.

## Phase 3 — Beyond HTML/JS/CSS: installable, offline-capable bots (PWA)

Items 1-3 confirmed; native app wrapper (item 4) dropped for now — a step too far given the "keep it simple, keep it virtual" direction.

1. Add a `manifest.json` + a couple of icon sizes per bot (or one shared manifest keyed by bot, simplest: one per bot since each is already a standalone URL).
2. A minimal service worker caching the bot's own HTML + the dice-box/theme assets it needs, so a bot keeps working at the table with no wifi once opened once.
3. "Add to Home Screen" becomes a real installable app icon, not just a bookmark — this is the single biggest perceived-polish jump for someone actually using these at a game table.

Effort: medium (service worker caching needs care per bot's external assets, e.g. dice-box CDN/self-hosted assets, fonts).

## Phase 4 — Prioritize with data, not guesses

`assets/analytics.js` / Firebase already track usage. Before investing Phase 1/2 effort broadly:
1. Pull which bots get opened/played most.
2. Order the Phase 1 (flavor text/assets) and Phase 2 (sensory) rollout by that ranking instead of alphabetical/arbitrary order.

Effort: low (read existing data), high leverage on where to spend the rest of the plan's effort.

## Phase 5 — Removed

Physical/print companion materials are out of scope per directive: boardbots stays simple and 100% virtual, no physical-world deliverables.

## Open decisions needed before implementation

1. Which phase(s) to start with — recommend Phase 0 first (safety net + the standing skill-file instruction), then Phase 1 asset-sourcing pass + Phase 2 SFX pass, prioritized by Phase 4 usage data.
2. PWA/offline (Phase 3): confirm scope — per-bot install, or a single "shell" app that can load any bot offline.
3. Asset licensing bookkeeping: confirm the CC0-first / credit-CC-BY approach above is acceptable, and that `credits.html` is the right place to log every non-CC0 asset used.

## Validation checklist (applies to every phase)

- `node --check` on every modified bot's extracted inline script.
- Live browser pass: desktop 1440px and mobile 390px, zero console/page errors, no horizontal overflow.
- Existing game logic/state persistence/solo-mode behavior unchanged (exercise a full turn cycle).
- New versioned file created before any substantial pass on a shipped bot (never edit the shipped version in place).
- Every new texture/icon/audio/model asset has a confirmed free license (CC0 preferred) and, if attribution-required, a corresponding entry added to `credits.html`.
- Deploy to `staging` first; promote to `main` only on explicit request.
