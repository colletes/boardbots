name = "ludoproxy"
main = "ludoproxy.js"
compatibility_date = "2026-01-01"

# Deploy with: cd workers && wrangler deploy
# Then set the secret once: wrangler secret put LUDOPEDIA_TOKEN
# Must end up reachable at the URL configured as LUDOPEDIA_PROXY_BASE in
# tools/tierlist.html (defaults to https://ludoproxy.boardbots.workers.dev).
